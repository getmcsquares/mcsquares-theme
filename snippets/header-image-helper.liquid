<script type="text/javascript">
    var applyImage = function (index) {
        var index = index
        var index0 = index - 1
        {% assign block = blocks[index0] %}
        console.log('index:', index0, {{ block.settings.title }})
        windowsize = $(window).width();
        if (windowsize < 750) {
            var mobileimage = "{{ block.settings.image_mobile | img_url: '800x800' }}"
            console.log( 'init mobile:', index0, mobileimage )
            //$(".background-target-" + index).attr("src", "{{block.settings.image_mobile | img_url: '800x800'}}");
            if ( index > 0 ){
                $(".background-target-" + index).css("background-image", "url(" + mobileimage + ")");
            } else {
                $(".background-target-" + index).css("background-image", "url(" + mobileimage + ")");
            }
        } else if (windowsize >= 750){
            var image = "{{ block.settings.background_image | img_url: '1800x1800' }}"
            var currentBG = $(".background-target-" + index).css("background-image");
            if ( index > 0 ){
                if( currentBG != image ){
                    $(".background-target-" + index).css("background-image", "url(" + image + ")");
                }
            } else {
                if( currentBG != image ){
                    $(".background-target-" + index).css("background-image", "url(" + image + ")");
                }
            }
            } else {
            var image = "{{ block.settings.background_image | img_url: '1800x1800' }}"
            var currentBG = $(".background-target-" + index).css("background-image");
            if ( index ){
                if( currentBG != image ){
                    $(".background-target-" + index).css("background-image", "url(" + image + ")");
                }
            } else {
                if( currentBG != image ){
                    $(".background-target-" + index).css("background-image", "url(" + image{{forloop.index}} + ")");
                }
            }
        }
    };
    var applyTitleImage = function () {
        windowsize = $(window).width();
        if (windowsize < 992) {
            $(".titleimg").attr("src", "{{block.settings.titleimg_mobile | img_url: '500x500'}}");
        } else if (windowsize >= 992){
            $(".titleimg").attr("src", "{{block.settings.titleimg | img_url: '1800x1800' }}");
        }else {
            $(".titleimg").attr("src", "{{block.settings.titleimg | img_url: '1800x1800'}}");
        }
    }
    $(window).resize(function() {
        for (var i = 1; i <= {{ section.blocks.size }}; i++ ){
            applyImage(i)
        }
        applyTitleImage()
    });
    $(document).ready(function() {
        for (var i = 1; i <= {{ section.blocks.size }}; i++ ){
            applyImage(i)
        }
        applyTitleImage()
    });
</script>
