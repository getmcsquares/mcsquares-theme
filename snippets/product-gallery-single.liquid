{%- capture classes -%}
  product-gallery--slider
  {% if product.images.size > 0 %}
    product-gallery--has-images
    product-gallery--style-square
  {% endif %}
{%- endcapture -%}

<!-- <div
  class="{{ classes | strip_newlines }}"
  data-product-slider
  aria-label="{{ product.title }} image. Click or Scroll to Zoom."
> -->
<div
  class="{{ classes | strip_newlines }}"
  aria-label="{{ product.title }} image. Click or Scroll to Zoom."
>
{% for filterImage in product.images %}
{%- unless image.alt contains 'logo' -%}
{% assign image = filterImage %}
{% break %}
{%- endunless -%}
{% endfor %}
  <figure
    class="
      product-gallery--image
      image--selected
    "
    data-product-image="{{ image.id }}"
    data-product-image-index="0"
    tabindex="-1"
    {% if selected_image != image %}
      aria-hidden="true"
    {% endif %}
  >
    <div
      class="product-galley--image-background"
        {%
          include 'rimg',
          img: image,
          size: 'x600',
          lazy: false,
          background: true
        %}
    >
    </div>
  </figure>
</div>
