
{% assign bg_color = section.settings.bg_color %}
{% include 'svg-curve-transparent' %}
<div style="background-color: {{ bg_color }}">
<section class="container">
  {% if section.settings.title != blank %}
    <h2 class="home-section--title">
      {{ section.settings.title | escape }}
    </h2>
  {% endif %}
  <div class="home-section--content">
  {% for block in section.blocks %}
    <div class="custom-review row">
      <div class="col-2 d-flex align-items-start">
        <img src="{{ block.settings.avatar | img_url: '80x' }}" style="max-width: 100%; border-radius: 50%;">
      </div>
      <div class="col">
        <p style="width:100%;"><span class="tex-blue">{{block.settings.author}}</span> <span class="text-small text-grey">Verifed Buyer</span><span class="review-date text-grey" style="float:right">{{ block.settings.date }}</span></p>
        <span class="stars"></span>
        <h5>{{block.settings.heading}}</h5>
        <p>{{block.settings.copy}}</p>
      </div>
    </div>
  {% endfor %}
  </div>
</section>
</div>
{% schema %}
{
  "name": "Reviews Products",
  "class": "index--section",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Section Background"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Section Title (optional)"
    }
  ],
  "blocks": [
    {
      "type": "quote",
      "name": "Custom Review",
      "settings": [
        {
          "type": "text",
          "id": "author",
          "label": "Author",
          "default": "Gina G."
        },
        {
          "type": "text",
          "id": "date",
          "label": "Date (text)",
          "default": "10/14/19"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Review Heading",
          "default": "Amazing!"
        },
        {
          "type": "text",
          "id": "copy",
          "label": "Review Copy",
          "default": "This is incredible. I recommend it!"
        },
        {
          "type": "image_picker",
          "id": "avatar",
          "label": "Reviewer Avatar"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Reviews Blocks Product",
      "category": "Reviews"
    }
  ]
}

{% endschema %}